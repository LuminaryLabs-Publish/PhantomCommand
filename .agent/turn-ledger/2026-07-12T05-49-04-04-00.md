# Turn Ledger

**Timestamp:** `2026-07-12T05-49-04-04-00`  
**Repository:** `LuminaryLabs-Publish/PhantomCommand`

## Goal

Record the campaign bootstrap and Continue-resume authority gap without modifying runtime behavior.

## Completed

- Compared ten accessible Publish repositories with nine eligible central ledgers.
- Excluded `LuminaryLabs-Publish/TheCavalryOfRome`.
- Selected `LuminaryLabs-Publish/PhantomCommand` as the oldest eligible repository.
- Read `src/menu/graveyard-menu.js`, `src/campaign/campaign-scene.js`, `game.html`, `package.json` and current `.agent` state.
- Identified the interaction loop, domains, 20 implemented kits and services.
- Confirmed the menu emits `campaign=new` and `campaign=continue`.
- Confirmed the campaign ignores both route intent and stored state.
- Confirmed Continue is enabled by raw presence across three keys.
- Confirmed victory writes an unversioned, minimal and unconsumed payload.
- Defined `phantom-command-campaign-bootstrap-resume-authority-domain`.
- Added bootstrap, save-envelope, hydration, result, observation and fixture contracts.
- Refreshed the required root `.agent` files.

## Not changed

```txt
runtime source
gameplay behavior
persistence behavior
navigation
rendering
audio
package scripts
dependencies
deployment
```

## Validation status

```txt
source inspection: complete
JSON registry replacement: written
npm run check: not run
npm run build: not run
browser route/storage smoke: not run
resume fixtures: not implemented
branch: main only
pull request: none
```

## Main conclusion

The current Continue affordance is a route label, not a resume capability. It requires validated storage admission, deterministic hydration, an atomic bootstrap result and first resumed-frame proof.