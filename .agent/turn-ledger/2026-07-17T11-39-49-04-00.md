# Turn Ledger — Campaign Camera Coverage Bounds

**Timestamp:** `2026-07-17T11-39-49-04-00`  
**Repository:** `LuminaryLabs-Publish/PhantomCommand`  
**Branch:** `main`  
**Change class:** documentation-only audit

## Summary

Selected PhantomCommand as the oldest synchronized eligible `LuminaryLabs-Publish` repository after comparing the complete organization inventory with central ledger and root `.agent` coverage.

The run documents a camera-boundary policy gap: the circular arena has radius `147`, but the camera center uses independent square clamps and no zoom-aware visible-footprint constraint. The resulting camera generation is rendered without a typed boundary result or matching frame acknowledgement.

## Completed

- Compared 11 Publish repositories.
- Excluded `LuminaryLabs-Publish/TheCavalryOfRome`.
- Confirmed ten eligible ledgers, root `.agent` states and synchronized heads.
- Selected and modified only `LuminaryLabs-Publish/PhantomCommand`.
- Identified the complete menu and campaign interaction loops.
- Identified all active domains.
- Preserved the 20-kit service census.
- Traced the arena, camera, zoom, source viewport and isometric transforms.
- Added architecture, render, gameplay, interaction, camera-bounds, deploy and central-sync audits.
- Refreshed the root `.agent` indexes and machine registry.
- Changed no runtime, tests, workflow or deployment behavior.

## Main finding

```txt
outer ring radius: 147
camera x range: -147..147
camera z range: -147..147
maximum admitted camera-center radius: 207.89
zoom-aware visible-footprint constraint: absent
CameraCoverageResult: absent
FirstCameraBoundsFrameAck: absent
```

## Proposed authority

`phantom-command-campaign-camera-coverage-bounds-authority-domain`

## Validation boundary

Source inspection only. No browser fixture, build, artifact smoke or Pages-origin smoke was executed. No runtime camera correction or production-readiness claim is made.