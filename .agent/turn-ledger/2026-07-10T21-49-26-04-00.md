# PhantomCommand Turn Ledger

**Timestamp:** `2026-07-10T21-49-26-04-00`

## Goal

Document one selected Publish repository and leave an implementation-safe lifecycle boundary while preserving the existing Continue-resolver and action-authority priorities.

## Completed

- Selected only `LuminaryLabs-Publish/PhantomCommand` after the full Publish-to-central-ledger comparison.
- Excluded `TheCavalryOfRome`.
- Re-read `index.html`, `src/menu/graveyard-menu.js`, `src/menu/crt-renderer.js`, `src/campaign/campaign-scene.js`, `package.json`, and current `.agent` state.
- Reconfirmed the interaction loop, domains, kits, and kit services.
- Verified that both menu and campaign start recursive RAF loops without retained request IDs.
- Verified that route listeners are installed without a coordinated removal ledger.
- Verified that `createCrtRenderer()` returns live WebGL state but no disposal service.
- Verified that menu audio closes only when ambience is explicitly disabled.
- Verified that campaign restart and exit rely on reload/navigation rather than session lifecycle commands.
- Added timestamped architecture, render, gameplay, interaction, lifecycle, and deploy audits.
- Refreshed the root `.agent` files and kit registry.
- Pushed directly to `main`; no branch or pull request was created.
- Updated the central repository ledger and internal change log.

## Decision

The implementation queue remains:

```txt
Continue resolver
  -> action-result authority
  -> runtime-session lifecycle authority
  -> versioned resume fidelity
```

The new lifecycle slice should be additive. It must preserve current routes, visuals, controls, fixed-step constants, legacy host fields, and page-navigation behavior while making teardown and remount behavior explicit and testable.

## Validation

Documentation-only pass. Runtime checks and browser/GPU lifecycle fixtures were not run because the required lifecycle services and fixtures do not exist yet.