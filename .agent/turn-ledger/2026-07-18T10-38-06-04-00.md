# Turn Ledger — Campaign Target Query Work Budget

**Timestamp:** `2026-07-18T10-38-06-04-00`  
**Repository:** `LuminaryLabs-Publish/PhantomCommand`  
**Branch:** `main`  
**Status:** `campaign-target-query-work-budget-authority-audited`

## Summary

PhantomCommand was selected as the oldest synchronized eligible Publish repository after excluding Cavalry of Rome and confirming that all ten eligible repositories remain centrally tracked, have root `.agent` state and match their documented heads.

This turn inspected `src/campaign/campaign-scene.js`, the menu and campaign static checks, the current root `.agent` state and the central PhantomCommand ledger. The focused finding is repeated reconstruction of enemy and ally arrays inside the fixed-step target-query path.

## Completed

- Compared the full 11-repository Publish inventory.
- Selected only PhantomCommand.
- Identified the menu, campaign, simulation and render loops.
- Preserved all 20 implemented kits and their services.
- Traced unit, tower and projectile target queries.
- Derived the pre-wave idle source-construction floor.
- Added architecture, render, gameplay, interaction, simulation-query, deploy and central-sync audits.
- Refreshed the required root `.agent` documents and machine registry.

## Finding

```txt
initial player units: 6
initial enemies: 0
enemies() calls per pre-wave idle tick: 6
source-visible arrays per enemies() call: 2
source-visible query arrays per idle tick: 12
conditional source-visible query arrays at 60 ticks: 720 / second
additional arrays per tower per tick: 2
```

This is source arithmetic only. No profiler, heap, GC, frame-time, browser, artifact or Pages fixture was run.

## Proposed authority

`phantom-command-campaign-target-query-work-budget-authority-domain`

The proposed authority owns team-index settlement, target-query admission, reusable query views, work observations, budget settlement, stale-generation rejection and matching frame proof.

## Boundary

Documentation changed. Runtime behavior, combat, targeting, rendering, persistence, tests, build and deployment did not change.