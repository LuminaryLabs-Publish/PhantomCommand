# Turn Ledger — PhantomCommand

**Timestamp:** `2026-07-10T11-10-08-04-00`

## Work performed

```txt
- Refreshed public Publish repo comparison.
- Excluded TheCavalryOfRome.
- Compared central ledger recency across non-Cavalry Publish repos.
- Selected PhantomCommand as the oldest eligible documented fallback.
- Read repo-local .agent root docs and kit registry.
- Read package.json, game.html, src/menu/graveyard-menu.js, and src/campaign/campaign-scene.js.
- Added this timestamped tracker and audit set.
- Refreshed root .agent docs.
- Updated central repo ledger and internal change-log.
```

## Decision

```txt
selectedRepo: LuminaryLabs-Publish/PhantomCommand
selectionReason: oldest eligible fallback after no new, ledger-missing, .agent-missing, recently added, or otherwise undocumented non-Cavalry repo was found
excludedRepo: LuminaryLabs-Publish/TheCavalryOfRome
branch: main
runtimeSourceChanged: no
```

## Finding

`PhantomCommand` has an active campaign route, but the proof surface is still aggregate and inline. `campaign-scene.js` should be converted only after source rows, action-result rows, simulation summaries, render readback, and fixture diagnostics exist.

## Next safe ledge

```txt
PhantomCommand Campaign Fixture Readback Ledger Refresh + GameHost Gate
```
