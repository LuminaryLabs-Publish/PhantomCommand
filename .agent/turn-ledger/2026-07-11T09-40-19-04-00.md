# Turn Ledger

**Timestamp:** `2026-07-11T09-40-19-04-00`

## Goal

Audit one eligible Publish repository and record the next deterministic architecture boundary.

## Repository

```txt
LuminaryLabs-Publish/PhantomCommand
branch: main
runtime changes: none
```

## Work completed

- Compared the ten accessible Publish repositories with nine eligible central ledger entries.
- Excluded `TheCavalryOfRome`.
- Skipped active same-window work on `HorrorCorridor`.
- Selected `PhantomCommand` as the oldest stable eligible fallback.
- Read `graveyard-menu.js`, `campaign-scene.js`, `crt-renderer.js`, package checks and current `.agent` records.
- Catalogued the interaction loop, domains, implemented kits, planned kits and services.
- Identified CRT display/input projection divergence and drag-selection visual mismatch.
- Added the timestamped audit set and refreshed root routing documents.

## Finding

The shader renders source coordinates through containment and radial CRT curvature. `screenToSource()` applies containment only. Pointer-derived menu and campaign actions can therefore target a different source/world point than the one visibly under the cursor.

## Next safe ledge

```txt
PhantomCommand CRT Display/Input Projection Authority
+ CPU/GLSL Projection and Visual Selection Fixture Gate
```

## Validation

```txt
runtime changed: no
branch created: no
pull request created: no
npm run check: not run
npm run build: not run
browser smoke: not run
projection fixtures: absent
```