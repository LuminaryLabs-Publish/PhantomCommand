# PhantomCommand Turn Ledger

**Timestamp:** `2026-07-15T08-41-37-04-00`  
**Status:** `public-diagnostic-capability-frame-admission-authority-audited`

## Summary

Selected PhantomCommand through the oldest synchronized eligible rule and audited its public diagnostic surface. The campaign publishes live mutable state and camera references plus direct mutation functions through `window.GameHost`, without a capability generation, caller lease, expected revision, typed result, retirement receipt or matching Canvas2D/CRT frame acknowledgement.

## Plan ledger

**Goal:** record one bounded documentation turn that identifies the complete repository architecture and isolates the public capability ownership gap.

- [x] Compare all accessible Publish repositories with the central ledger.
- [x] Exclude Cavalry of Rome.
- [x] Select only PhantomCommand.
- [x] Inspect campaign boot, input, update, render, public host and static checks.
- [x] Record the interaction loop, domains, 20 implemented kits and services.
- [x] Add architecture, render, gameplay, interaction, host-capability and deployment audits.
- [x] Refresh required root `.agent` state.
- [x] Push only to `main`; create no branch or pull request.
- [ ] Implement or execute runtime proof.

## Source evidence

```txt
src/campaign/campaign-scene.js
  -> state and camera are mutable runtime owners
  -> RAF update and render consume both owners
  -> window.GameHost publishes both objects directly
  -> window.GameHost also publishes startWave build and setZoom

scripts/check-campaign.mjs
  -> requires the GameHost marker
  -> does not validate capability restriction or typed settlement
```

## Result

```txt
required authority:
  phantom-command-public-diagnostic-capability-frame-admission-authority-domain

implemented kits retained: 20
planned authority surfaces: 19
runtime behavior changed: no
```
