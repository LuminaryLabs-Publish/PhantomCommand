# Turn Ledger: Menu Audio Activation and Lifecycle

**Timestamp:** `2026-07-12T07-29-32-04-00`  
**Repository:** `LuminaryLabs-Publish/PhantomCommand`  
**Branch:** `main`

## Summary

Selected `PhantomCommand` as the oldest stable eligible Publish repository. Documented the menu Web Audio lifecycle gap without changing runtime code.

## Plan ledger

**Goal:** define one session-scoped authority for AudioContext activation, state recovery, graph replacement and ordered teardown.

- [x] Compared the full Publish organization inventory with central tracking.
- [x] Excluded `TheCavalryOfRome`.
- [x] Confirmed nine eligible repos have ledger and root `.agent` coverage.
- [x] Reconciled repo-local timestamps before selection.
- [x] Inspected `src/menu/graveyard-menu.js`, package checks and current audit state.
- [x] Cataloged the interaction loop, domains, 20 implemented kits and services.
- [x] Added architecture, render, gameplay, interaction, audio and deploy audits.
- [x] Refreshed root `.agent` entrypoints and machine registry.
- [x] Used `main` only and created no branch or pull request.
- [ ] Runtime authority and browser fixtures remain unimplemented.

## Finding

`ensureAudio()` treats a non-null `state.audio` as sufficient even when its context may be suspended, interrupted or closed. No resume path, context-state observation, generation identity or statechange handling exists. `stopAmbience()` clears the current reference before an untracked 300 ms delayed close, so rapid off/on can overlap graph generations. Navigation, pagehide and visibility loss have no explicit retirement transaction.

## Files

See `.agent/trackers/2026-07-12T07-29-32-04-00/project-breakdown.md` for the complete inventory, contracts and validation boundary.

## Validation

Documentation only. No runtime, package, dependency, rendering, persistence, audio or deployment behavior changed.