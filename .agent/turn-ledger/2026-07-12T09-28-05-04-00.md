# PhantomCommand Turn Ledger

**Timestamp:** `2026-07-12T09-28-05-04-00`

## Summary

Selected PhantomCommand through the oldest eligible fallback and documented a menu pointer-hit admission gap. Pointer misses currently execute the previously selected menu or settings action.

## Plan ledger

**Goal:** record the source finding, DSK boundary, output set and validation limits for this run.

- [x] Compared the complete Publish repository set with the central ledger.
- [x] Excluded Cavalry of Rome.
- [x] Changed one Publish repository only.
- [x] Identified interaction loop, domains, kits and services.
- [x] Added tracker, architecture, render, gameplay, interaction, pointer-input and deployment audits.
- [x] Refreshed required root `.agent` files.
- [x] Used `main` only and created no branch or pull request.
- [ ] Runtime implementation and fixtures remain pending.

## Finding

```txt
pointer hit test = Miss
  -> selected command remains unchanged
  -> pointerdown still activates selected command
```

## Required authority

```txt
phantom-command-menu-pointer-hit-admission-authority-domain
```

## Change boundary

```txt
documentation only
runtime source unchanged
menu and campaign behavior unchanged
validation commands not run
```