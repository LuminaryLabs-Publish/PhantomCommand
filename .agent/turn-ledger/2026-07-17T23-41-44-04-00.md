# Turn Ledger — Menu Pointer Target Admission

**Timestamp:** `2026-07-17T23-41-44-04-00`  
**Repository:** `LuminaryLabs-Publish/PhantomCommand`  
**Branch:** `main`

## Selection

The current `LuminaryLabs-Publish` organization inventory contains 11 repositories. `LuminaryLabs-Publish/TheCavalryOfRome` was excluded. The remaining ten repositories were present in `LuminaryLabs-Dev/LuminaryLabs`, had root `.agent` state and matched their documented heads. PhantomCommand was selected as the oldest synchronized eligible repository.

## Work completed

- Re-read the main-menu runtime, CRT source mapping, static menu check and current `.agent` state.
- Preserved the complete 20-kit implemented inventory and service descriptions.
- Identified unconditional pointer activation after main-menu and settings-panel hit-test misses.
- Added architecture, render, gameplay, interaction, menu-input, deployment and central-sync audits.
- Refreshed the required root `.agent` documents and machine kit registry.
- Changed documentation only.

## Focused finding

```txt
pointer press
  -> screenToSource
  -> menuHitIndex or panelHitIndex
  -> hit-test miss can return -1
  -> selected row remains unchanged
  -> activateMain or activatePanel still runs
  -> pointer action inherits stale keyboard selection
```

## Proposed authority

`phantom-command-menu-pointer-target-admission-authority-domain`

## Validation boundary

No runtime, pointer behavior, menu behavior, gameplay, renderer, test, workflow, build or deployment implementation changed. Executable browser, artifact and Pages fixtures remain absent.