# Source profile audit: legacy construct profile demotion

Timestamp: 2026-07-10T14-11-51-04-00

## Legacy source profile

`construct-spiral-intro-kit` and related construct proof files still exist and remain useful as a separate kit proof target.

They are not the live campaign route authority.

## Current live authority

The live gameplay route is:

```txt
game.html
  -> src/campaign/campaign-scene.js
```

That file owns the grave-ring campaign descriptors, actions, simulation, render projection, save-on-win, and `GameHost`.

## Do not do next

```txt
Do not delete the legacy construct kit.
Do not treat construct-profile smoke as campaign proof.
Do not start source-profile parity before campaign source/action/result fixture proof.
Do not expand construct intro while live campaign action results are still implicit.
```

## Next safe interpretation

Keep the construct kit as legacy support. The next implementation should make the campaign route itself fixture-readable through source descriptors, action-result rows, simulation summaries, render readback, and additive GameHost diagnostics.
