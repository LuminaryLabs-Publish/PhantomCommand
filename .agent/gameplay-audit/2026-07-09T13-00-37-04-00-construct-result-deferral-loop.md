# PhantomCommand Construct Result Deferral Loop

**Timestamp:** `2026-07-09T13-00-37-04-00`

## Current playable loop

```txt
open menu
  -> enter game scene
  -> watch construct assemble
  -> pan/zoom while construct forms
  -> skip or restart
  -> construct reaches command online
```

## Current gameplay authority

The current scene is a construct proof, not a full RTS or scenario game.

The gameplay-adjacent authority is the live construct sequence:

```txt
BUILD_ID smooth-ring-handoff-v6
RING_COUNT 10
ring gaps 0
ring part counts [5,5,5,5,6,8,10,12,16,20]
piece count 92
ring handoff 0.72
part stagger 0.025
total build time about 19.923 seconds
```

## Why construct result stays deferred

There is not yet a source-owned profile proving that the live browser runtime matches a deterministic descriptor set. Until that exists, a construct-result domain would only wrap inline browser behavior instead of consuming a source contract.

## Required precondition before construct result work

```txt
source profile fixture passes
ring descriptor fixture passes
piece descriptor fixture passes
timeline fixture passes
legacy GameHost compatibility passes
additive sourceProfile diagnostics exist
build fixture gate exists
central ledger points at matching tracker/audit files
```

## Not next

```txt
scenario bootstrap
RTS commands
unit control
economy
combat waves
renderer rewrite
```
