# Pointer Lifecycle Browser Fixture Gate

Required source, built-output and Pages fixtures:

- primary drag released outside canvas
- middle pan released outside canvas
- `pointercancel`
- `lostpointercapture`
- blur and hidden-document settlement
- route exit during a gesture
- stale move/release after retirement
- two simultaneous pointer ids
- unsupported pointer-capture fallback
- first matching completed/cancelled visible frame

No fixture is currently available or executed.
